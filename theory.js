const questions1 = "What is two way binding?";
const answers1 =
  "Update and display property on same time is called two way binding.";
const pipes = "What is pipes ?";
const answerPipe = "Pipe is used to format data we can not use pipe in ts file";
const CallApi = "How to call api in angular ?";
const CallApiAnswers =
  "1. We create a service by terminal 2.Import httpClient module from @angular/common/http in service file 3. create one function  and return this.http.get(url) 4. import service to any ts file and call in constructor function by the help of subscriber";
